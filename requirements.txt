
llama-index-core==0.10.68
llama-index-readers-file==0.1.33
llama-index-vector-stores-qdrant==0.2.14
llama-index-postprocessor-cohere-rerank==0.1.7
llama-index-embeddings-openai==0.1.11
llama-index-llms-openai==0.1.31
llama-index-retrievers-bm25==0.1.5
llama-parse==0.4.9

# LLM + embeddings
openai==1.55.0

# Vector database
qdrant-client==1.11.3
fastembed==0.8.0

# Backend API
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
python-multipart==0.0.17

# Frontend
streamlit==1.40.1

# Evaluation
ragas==0.2.6
datasets==3.1.0

# Observability
# Langfuse 2.x is the line that integrates with llama-index-core 0.10.x via
# set_global_handler("langfuse"). Do NOT bump either of these: langfuse 3.x and
# llama-index-callbacks-langfuse 0.2.0+ target the new instrumentation API and
# require llama-index-core >= 0.11, which would drag core from 0.10.68 to 0.14.
langfuse==2.60.10
llama-index-callbacks-langfuse==0.1.6
# DEPRECATED: not wired into this project. LangSmith's automatic tracing hooks
# LangChain runnables, and this codebase is LlamaIndex-only, so
# LANGCHAIN_TRACING_V2 captures nothing here. Kept because ragas pulls it in
# transitively — removing it would break the eval dependency chain.
langsmith==0.1.147

# Utilities
python-dotenv==1.0.1
loguru==0.7.2
httpx==0.27.2
tenacity==8.5.0
rich==13.9.4

# ── Auth (JWT + password hashing) ─────────────────────────────
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.0.1

# ── Cache + worker (Redis, ARQ) ───────────────────────────────
redis==5.3.1
hiredis==3.4.0
arq==0.28.0
