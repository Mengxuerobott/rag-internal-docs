# Development and CI dependencies.
#
# Deliberately kept out of requirements.txt: Dockerfile.api installs that file
# into the deployed image, and the test runner has no business shipping to
# production.
#
# Install alongside the runtime deps:
#     pip install -r requirements.txt -r requirements-dev.txt

pytest==8.3.3
# Required by the async webhook tests, which use @pytest.mark.asyncio.
# Without it those marks are silently ignored and the tests do not run.
pytest-asyncio==0.24.0
